import {
    createBrowserRouter,
    Link,
    RouterProvider,
} from "react-router";


type Props = {}

export const HomeButton = (props: Props) => {
    return (
        <>
            <Link to="/">Home</Link>
            
        </>
    )
}